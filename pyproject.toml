[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hermes-kortex"
version = "0.2.0"
description = "Project KORTEX — Experiential memory plugin for Hermes Agent"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [{name = "VeelaCleave"}]
classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-asyncio>=0.21"]

[project.entry-points."hermes_agent.plugins"]
kortex = "kortex:register"

[project.entry-points."hermes_agent.memory"]
kortex = "kortex:register"

[project.entry-points."hermes_agent.context_engine"]
kortex = "kortex:register"

[tool.setuptools.packages.find]
include = ["kortex*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
